<div class="container">
  <%= link_to "<<<", root_path, class:"btn btn-back mg-top-md" %>
  

  <div class="row-new">
  <h2 class="u-center-text">Log in</h2>
    <div class="col-lg-6">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
          <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
          </div>
          <div class="form-actions d-flex justify-content-end">
            <%= f.button :submit, "Log in", class: 'btn btn-success btn-md m-2' %>
          </div>
          <% end %>
            <div class="d-flex h-centered justify-content-end">
              <%= render "devise/shared/links" %> 
            </div>
    </div>
  </div>
</div>
